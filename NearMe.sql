DROP DATABASE IF EXISTS NearMe;
CREATE DATABASE NearMe;
USE NearMe;

CREATE TABLE userInfo(
    first_name VARCHAR(100) NOT NULL,
    last_name VARCHAR(100) NOT NULL,
    age INTEGER)
INSERT INTO userInfo(first_name,)